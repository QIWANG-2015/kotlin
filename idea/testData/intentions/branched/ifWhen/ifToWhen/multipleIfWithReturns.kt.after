fun foo(arg: Any?): Int {
    // 1
    when (arg) {
        is Int -> {
            return arg // 2
        }
        is String -> {
            return 42 // 4
        }
        null -> {
            // 6
            return 0
        }
        else -> {
            return -1 // 8
        }
    }
}