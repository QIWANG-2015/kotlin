// WITH_RUNTIME
fun foo(list: List<String>) {
    val result: String? = list.firstOrNull { s -> // search for first non-empty string in the list
        s.length > 0
    }// string should be non-empty
    // save it into result
}