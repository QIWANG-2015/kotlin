// WITH_RUNTIME
fun foo(list: List<String>): String? {
    <caret>val result: String? = list
            .flatMap { s -> s.lines() }
            .firstOrNull { line -> line.isNotBlank() }
    return result
}